# td-agent.conf: Managed by puppet.
#
<% @config.each do |plugin_name, plugin_configs| -%>
<% plugin_configs.each do |plugin_config| -%>
<<%= plugin_name %> <%= plugin_config.delete('tag_pattern') %>>
  <% plugin_config.each do |key, value| -%>
    <%= key %> <%= value %>
  <% end -%>
</<%= plugin_name %>>
<% end -%>
<% end -%>
